{
	"extends": "@sindresorhus/tsconfig",
	"compilerOptions": {
		"target": "esnext",
		// "noEmit": true, // Useful when running `tsc` directly

		// TODO: Get all these removed
		"allowJs": true,
		"declaration": false, // Needed because of `allowJs`
		"strict": false,
		"noImplicitReturns": false,
	},
	"include": [
		"source"
	]
}
